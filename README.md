The code in this repository shows how to submit multiple condor jobs with each 
job calling a MATLAB function with different parameter values. Moreover, each
job returns mat files with different filenames.